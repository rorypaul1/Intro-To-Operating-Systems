To run two c programs simultaneously in the same terminal:
    1. Change terminal to git bash
    2. Enter ./exectuable_name argument & ./exectuable_name argument &

To kill the programs:
    1. In powershell:
        "kill (pid of program)"
        OR
        "Stop-Process -Name (name of executable)"